<nav class="navbar bg-light">
  <div class="container-fluid d-flex justify-content-between align-items-center">
    <div class="d-flex align-items-center">
      <a class="navbar-brand text-dark" href="<%= root_path %>">Home</a>
    </div>
    <div class="d-flex align-items-center">
      <% if Current.user %>
        <%= link_to 'My profile', profile_path, class: 'navbar-brand text-dark' %>
        
        <%= form_with url: logout_path, method: :delete, local: true, class: 'd-inline-block m-1' do %>
          <%= submit_tag 'Logout', class: 'navbar-brand text-dark btn-link p-0 m-0 align-baseline', style: 'border: none; background: none;' %>
        <% end %>
      <% else %>
        <%= link_to 'Sign up', sign_up_path, class: 'navbar-brand text-dark' %>
        <%= link_to 'Sign in', sign_in_path, class: 'navbar-brand text-dark' %>
      <% end %>
      <form class="d-flex align-items-center" role="search" method="get" action="/blog_posts/search">
        <input class="form-control me-2" type="search" name="query" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-dark" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>